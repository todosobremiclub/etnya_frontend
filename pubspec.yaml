name: etnya_frontend
description: Aplicación móvil de Etnya Pilates
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0

  # 🔥 Firebase: base + mensajería push (como al principio)
  firebase_core: ^3.4.0
  firebase_messaging: ^15.0.4

  url_launcher: ^6.3.0
  font_awesome_flutter: ^10.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/logo.jpeg


